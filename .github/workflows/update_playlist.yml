name: Auto Update CricHD JSON

on:
  schedule:
    - cron: '*/10 * * * *'  # প্রতি ৩০ মিনিট পরপর রান হবে
  workflow_dispatch:        # ম্যানুয়ালি রান করার অপশন

permissions:
  contents: write  # গিটহাবে ফাইল কমিট ও পুশ করার অনুমতি দেওয়ার জন্য

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Playwright Browsers
        run: |
          playwright install chromium

      - name: Run main script to generate JSON
        run: python main.py

      - name: Commit and push if JSON changed
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "action@github.com"
          git add crichd_matches.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update crichd_matches.json [skip ci]" && git push)
